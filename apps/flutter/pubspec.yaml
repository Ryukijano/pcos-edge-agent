name: pcos_edge
description: PCOS Edge — on-device LLM app running Gemma 4 via LiteRT-LM with hardware acceleration.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_litert_lm: ^0.3.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
